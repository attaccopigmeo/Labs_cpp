#include <iostream>

int main() {
    int level = 0, current = 0, max_numb = 0;
    std::cout << "Enter level: ";
    std::cin >> level;
    std::cout << "Max number: ";
    std::cin >> max_numb;
    for (int i = 0; i < level; i++)
    {
        
    }
    }