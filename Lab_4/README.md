# Лабораторная работа № 4 (Шмидт Камилла, ИТ-7, Вариант - 10)

## Задание 1 - Triangle

### Условие - 1

Реализовать определение класса (поля, свойства, конструкторы (не менее трех),
перегрузку оператора вывода для вывода полей, заданный метод согласно варианту).
Протестировать все методы, включая конструкторы, все исходные данные вводятся с клавиатуры, не
забывайте проверять данные на корректность. В главной функции main показать работу всех функций с дружественным интерфейсом.
Поля:
`double a`
`double b`
`double c`

Определить, существует ли треугольник с
такими длинами сторон. Результат
должен быть типа `bool`.

### Решение - 1

### Тесты - 1

```Введите сторону a: 2
Введите сторону b: 6
Введите сторону c: 8
Треугольник не существует.
t2 (по умолчанию): Стороны треугольника: a = 0, b = 0, c = 0
t2 после установки значений: Стороны треугольника: a = 3, b = 4, c = 5
Треугольник существует.
t3 (копия t1): Стороны треугольника: a = 2, b = 6, c = 8
```

## Задание 2 - Triangle

### Условие - 2

Добавить к реализованному в первом задании классу указанные в варианте
перегруженные операции. В главной функции main показать работу всех функций с дружественным интерфейсом.
Унарные операции:
`-` вычисляется площадь треугольника, результатом должно быть вещественное число
Операции приведения типа:
`double` (неявная) - результатом является периметр треугольника;
`bool` (явная) - результатом является `true`, если треугольник с такими длинами сторон существует, и `false` в противном случае.
Бинарные операции:
`<` Triangle t - сравниваются площади треугольников;
`>` Triangle t - сравниваются площади треугольников.

### Решение - 2

### Тесты - 2

```Введите сторону a: 6
Введите сторону b: 7
Введите сторону c: 8

Треугольник t1: a = 6, b = 7, c = 8
Существует: Да
Периметр: 21
Площадь: 20.3332

Введите сторону a: 8
Введите сторону b: 9
Введите сторону c: 2

Треугольник t2: a = 8, b = 9, c = 2
Существует: Да
Периметр: 19
Площадь: 7.3101

t1 имеет большую площадь, чем t2.

t3: a = 3, b = 4, c = 5 Площадь: 6 Периметр: 12
t3 существует? Да
```

## Задание 3 - Money

### Условие - 3

Поля:
`unsigned int rubles`
`short int kopeks`

Добавление произвольного количества
копеек (`unsigned int`) к объекту типа
`Money`.
Результат должен быть типа `Money`.

### Решение - 3

### Тесты - 3

## Задание 4 - Money

### Условие - 4

Унарные операции:
`++` добавление копейки к объекту типа `Money`
`--` вычитание копейки из объекта типа `Money`
Операции приведения типа:
`unsigned int` (явная) - результатом является кол-во рублей (копейки отбрасываются);
`double` (неявная) - результатом является копейки, переведенные в рубли, целые рубли отбрасываются, результат < 1.
Бинарные операции:
`+` `Money m`, беззнаковое целое число (копейки). Результат должен быть типа `Money`
`-` `Money m`, беззнаковое целое число (копейки). Результат должен быть типа

### Решение - 4

### Тесты - 4

## Задание 5 - RightTriangle

### Условие - 5

Поля:
`double x`
`double y`

Вычислить расстояние от одной точки до
другой.
Результат должен быть типа double.

### Решение - 5

### Тесты - 5

## Задание 6 - RightTriangle

### Условие - 6

Унарные операции:
`++` увеличивает стороны треугольника в 2 раза (чтобы вновь получить катеты прямоугольного треугольника)
`--` уменьшает стороны треугольника в 2 раза (чтобы вновь получить катеты прямоугольного треугольника)
Операции приведения типа:
`double` (явная) - результатом является площадь треугольника, если треугольник существует и отрицательное число в противном случае;
`bool` (неявная) - результатом является `true`, если треугольник с такими длинами сторон существует и `false` в противном случае.
Бинарные операции:

### Решение - 6

### Тесты - 6

## Задание 7 - QuadraticEquation

### Условие - 7

Поля:
`double a`
`double b`
`double c`

Вычисление корней квадратного
уравнения. Результат должен быть
массивом величин типа double (в нем от 0
до 2-х элементов, в зависимости от
количества корней).

### Решение - 7

### Тесты - 7

## Задание 8 - QuadraticEquation

### Условие - 8

Унарные операции:
`++` увеличивает коэффициенты уравнения на 1;
`--` уменьшает коэффициенты уравнения на 1.
Операции приведения типа:
`double` (неявная) – результатом является дискриминант уравнения.
`bool` (явная) – результатом является `true`, если корни существуют и `false` в противном случае;
Бинарные операции:
`==` QuadraticEquation t — уравнения равны, если равны их коэффициенты;
`!=` QuadraticEquation t — уравнения не равны, если не равны их
коэффициенты.

### Решение - 8

### Тесты - 8

## Задание 9 - Money

### Условие - 9

Поля:
`unsigned int rubles`
`short int kopeks`

Вычитание переменной типа `Money`
(учесть, что денежная величина не может
быть меньше 0). Результат должен быть
типа `Money`.

### Решение - 9

### Тесты - 9

## Задание 10 - Money

### Условие - 10

Унарные операции:
`++` добавление копейки к объекту типа `Money`
`--` вычитание копейки из объекта типа `Money`
Операции приведения типа:
`unsigned int` (неявная) – результатом является количество рублей
(копейки отбрасываются);
`double` (явная) – результатом является копейки, переведенные в рубли,
целые рубли отбрасываются, результат <1.
Бинарные операции:
`-` беззнаковое целое число, результат должен быть типа `Money`.
`-` `Money m`, результат должен быть типа `Money`. 
Результат не может быть отрицательным.

### Решение - 10

### Тесты - 10

## Задание 11 - Time

### Условие - 11

Поля:
`short int hours`
`short int minutes`

Вычитание минут (`unsigned int`) из
объекта типа `Time` (учесть, что возможен
переход в какие-либо предыдущие сутки).
Результат должен быть типа `Time`.

### Решение - 11

### Тесты - 11

## Задание 12 - Time

### Условие - 12

Унарные операции:
`-` обнуление часов и минут в объекте типа `Time`.
`--` обнуление минут в объекте типа `Time`
Операции приведения типа:
`short int` (неявная) – результатом является количество часов (минуты
отбрасываются);
`bool` (явная) – результатом является `true`, если часы и минуты не равны
нулю, и `false` в противном случае.
Бинарные операции:
`==` `Time t` – результатом является `true`, если значение часов и минут левого
операнда равно соответствующим значениям правого операнда, и `false` – в
противном случае.
`!=` `Time t` – результатом является `true`, если значение часов и минут левого
операнда не равно соответствующим значениям правого операнда, и `false`
– в противном случае.

### Решение - 12

### Тесты - 12

## Задание 13 - Time

### Условие - 13

Поля:
`short int hours`
`short int minutes`

Вычитание времени (величины типа
`Time`) из объекта типа `Time` (учесть, что
возможен переход в предыдущие сутки).
Результат должен быть типа `Time`.

### Решение - 13

### Тесты - 13

## Задание 14 - Time

### Условие - 14

Унарные операции:
`--` вычитание минуты из объекта типа `Time`
Операции приведения типа:
`short int` (явная) – результатом является количество часов (минуты
отбрасываются);
`bool` (неявная) – результатом является `true`, если часы и минуты не равны
нулю, и `false` в противном случае.
Бинарные операции:
`+` беззнаковое целое число – добавление минут к времени.
`+` `Time t` – сложить два времени.

### Решение - 14

### Тесты - 14

## Задание 15 - LineSegment

### Условие - 15

Поля:
`double x`
`double y`

Определить пересечение заданных
отрезков. Результат должен быть типа
`LineSegment` (он равен `null`, если отрезки
не пересекаются).

### Решение - 15

### Тесты - 15

## Задание 16 - LineSegment

### Условие - 16

Унарные операции:
`!` установить соответствующую координату в 0, чтобы получился
правильный диапазон. Ответом должен быть объект `LineSegment`
Операции приведения типа:
`int` (неявная) – результатом является целая часть координаты `y`;
`double` (явная) – результатом является координата `x`.
Бинарные операции:
`+` целое число – увеличивается координата `х`, ответом должен быть
правильный диапазон;
`>` `LineSegment d` – результат равен `true`, если левый диапазон включает
правый, и `false` в противном случае.

### Решение - 16

### Тесты - 16